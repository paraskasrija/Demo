import {StyleSheet} from 'react-native';
import { _config } from '../../config';

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: _config._theme._color._backgroundColor
    }
});